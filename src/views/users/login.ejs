<% layout('layouts/layout') %> <% layout('layouts/layout') %> <%
layout('layouts/layout') %>

<div class="container py-5">
  <div class="row justify-content-center">
    <div class="col-md-8 col-lg-6">
      <div class="card shadow-lg border-0 rounded-4">
        <div class="card-body p-4 p-md-5">
          <!-- Header Section -->
          <div class="text-center mb-4">
            <div
              class="bg-primary bg-gradient p-3 rounded-circle d-inline-block mb-3"
            >
              <i class="bi bi-person-plus-fill text-white fs-1"></i>
            </div>
            <h1 class="fw-bold mb-2">Welcome</h1>
            <p class="text-muted">LogIn to continue your camping adventures</p>
          </div>

          <!-- login Form -->
          <form
            action="/users/login"
            method="POST"
            class="validateForm"
            novalidate
          >
            <!-- Username -->
            <div class="mb-3">
              <label for="username" class="form-label">Username</label>
              <input
                type="text"
                class="form-control"
                id="username"
                name="username"
                placeholder="Enter username"
                required
              />
              <div class="invalid-feedback">Please enter your username.</div>
            </div>

            <!-- Password -->
            <div class="mb-4">
              <label for="password" class="form-label">Password</label>
              <input
                type="password"
                class="form-control"
                id="password"
                name="password"
                placeholder="........"
                required
              />
            </div>

            <!-- Submit Button -->
            <div class="d-grid mb-3">
              <button class="btn btn-primary btn-lg fw-bold py-2" type="submit">
                <i class="bi bi-person-plus me-2"></i>Log In
              </button>
            </div>

            <!-- Login Link -->
            <div class="text-center">
              <p class="text-muted mb-0">
                Don't have an account?
                <a
                  href="/users/register"
                  class="text-primary fw-semibold text-decoration-none"
                  >Register</a
                >
              </p>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>
